[
  { "id": "arith-precedence", "formula": "=1+2*3", "expected": 7, "note": "* binds tighter than +" },
  { "id": "arith-parens", "formula": "=(1+2)*3", "expected": 9, "note": "parentheses override precedence" },
  { "id": "arith-left-assoc", "formula": "=10-2-3", "expected": 5, "note": "subtraction is left-associative: (10-2)-3, not 10-(2-3)" },
  { "id": "arith-power", "formula": "=2^10", "expected": 1024, "note": "^ is exponentiation" },
  { "id": "arith-percent", "formula": "=50%", "expected": 0.5, "note": "postfix % divides by 100" },
  { "id": "ref-add", "formula": "=A1+A2", "expected": 30, "note": "fixture: A1 = 10, A2 = 20" },
  { "id": "range-sum", "formula": "=SUM(A1:A5)", "expected": 150, "note": "fixture: A1:A5 = 10, 20, 30, 40, 50" },
  { "id": "range-average", "formula": "=AVERAGE(A1:A5)", "expected": 30, "note": "mean of 10…50" },
  { "id": "range-min", "formula": "=MIN(A1:A5)", "expected": 10, "note": "smallest of 10…50" },
  { "id": "range-count", "formula": "=COUNT(A1:B5)", "expected": 5, "note": "COUNT ignores the text in B1:B3 and the blanks in B4:B5" },
  { "id": "range-counta", "formula": "=COUNTA(A1:B5)", "expected": 8, "note": "COUNTA counts the five numbers and the three text cells" },
  { "id": "sum-text-ref", "formula": "=SUM(A1:A5,B1)", "expected": 150, "status": "known-failing", "note": "Excel ignores text in a referenced cell (only literal text arguments are coerced); OneSheet coerces the single-cell ref B1 = \"hello\" like a literal and returns #VALUE!" },
  { "id": "sumif", "formula": "=SUMIF(A1:A5,\">25\")", "expected": 120, "note": "30 + 40 + 50" },
  { "id": "countif", "formula": "=COUNTIF(A1:A5,\">=30\")", "expected": 3, "note": "30, 40, 50" },
  { "id": "text-len", "formula": "=LEN(B1)", "expected": 5, "note": "fixture: B1 = hello" },
  { "id": "text-left", "formula": "=LEFT(B1,2)", "expected": "he", "note": "first two characters of hello" },
  { "id": "text-concat", "formula": "=CONCAT(B1,\" \",B3)", "expected": "hello World", "note": "fixture: B3 = World" },
  { "id": "text-upper", "formula": "=UPPER(B1)", "expected": "HELLO", "note": "case conversion" },
  { "id": "text-trim", "formula": "=TRIM(B2)", "expected": "wide open", "note": "fixture: B2 = \"  wide   open  \"; TRIM strips the ends and collapses inner runs to one space" },
  { "id": "text-amp", "formula": "=\"Total: \"&SUM(A1:A5)", "expected": "Total: 150", "note": "& concatenates and renders the number as text" },
  { "id": "text-coerce", "formula": "=C1+1", "expected": 43, "note": "fixture: C1 = '42 (text); arithmetic coerces numeric text" },
  { "id": "logic-if", "formula": "=IF(A1>5,\"big\",\"small\")", "expected": "big", "note": "10 > 5" },
  { "id": "logic-and", "formula": "=AND(A1>5,A2>5)", "expected": true, "note": "both comparisons hold" },
  { "id": "logic-iferror", "formula": "=IFERROR(1/0,\"safe\")", "expected": "safe", "note": "the #DIV/0! is swallowed" },
  { "id": "logic-if-lazy", "formula": "=IF(FALSE,1/0,42)", "expected": 42, "note": "the untaken branch is never evaluated, so its error never surfaces" },
  { "id": "vlookup-exact", "formula": "=VLOOKUP(\"banana\",A11:B14,2,FALSE)", "expected": 0.5, "note": "fixture: A11:B14 = apple 1.25 · banana 0.5 · cherry 3 · durian 7" },
  { "id": "vlookup-approx", "formula": "=VLOOKUP(35,A1:A5,1,TRUE)", "expected": 30, "note": "approximate match returns the largest value ≤ 35 in a sorted column" },
  { "id": "index-match", "formula": "=INDEX(B11:B14,MATCH(\"durian\",A11:A14,0))", "expected": 7, "note": "the classic INDEX/MATCH pair" },
  { "id": "xlookup", "formula": "=XLOOKUP(\"apple\",A11:A14,B11:B14)", "expected": 1.25, "note": "modern exact lookup" },
  { "id": "err-div0", "formula": "=1/0", "expected": "#DIV/0!", "note": "division by zero is a value, not a crash" },
  { "id": "err-name", "formula": "=NOPE(1)", "expected": "#NAME?", "note": "unknown function" },
  { "id": "err-ref", "formula": "=INDEX(A1:A5,9)", "expected": "#REF!", "note": "row 9 is outside a 5-row range" },
  { "id": "err-na", "formula": "=VLOOKUP(\"kiwi\",A11:B14,2,FALSE)", "expected": "#N/A", "note": "exact match with no hit" },
  { "id": "err-cycle", "formula": "=C31", "expected": "#CYCLE!", "note": "fixture: C31 = =C30 and this formula lives in C30, so the two cells reference each other; OneSheet reports its own #CYCLE! code (Excel warns and shows 0, Sheets shows #REF!)" },
  { "id": "date-serial", "formula": "=DATE(2026,9,7)", "expected": 46272, "note": "Excel serial for 7 Sep 2026 (day 1 = 1 Jan 1900)" },
  { "id": "date-days", "formula": "=DAYS(DATE(2026,12,25),DATE(2026,9,7))", "expected": 109, "note": "days from 7 Sep to 25 Dec 2026" },
  { "id": "date-rollover", "formula": "=DATE(2026,2,29)", "expected": 46082, "note": "2026 is not a leap year, so DATE rolls 29 Feb over to 1 Mar 2026 exactly as Excel does" },
  { "id": "round-half", "formula": "=ROUND(2.345,2)", "expected": 2.35, "note": "2.345 is 2.34499… in binary; a correct ROUND still gives 2.35" },
  { "id": "ai-mock", "formula": "=AI(\"summarize this\",\"the quick brown fox jumps over the lazy dog again and again\")", "expected": "the quick brown fox jumps over the lazy…", "note": "Mock Mode (no API key) is deterministic: summarize keeps the first eight words and appends an ellipsis" },
  { "id": "fill-absolute", "formula": "=$A$1+A1", "shift": [1, 1], "expected": "=$A$1+B2", "note": "what the fill handle writes one row down and one column right: the $-pinned reference holds, the relative one moves" }
]
